{
  "name": "@suid/system",
  "version": "0.0.0",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "jest",
    "watch": "tsc --build -w"
  },
  "dependencies": {
    "@suid/css": "0.0.0",
    "@suid/types": "0.0.0",
    "@suid/utils": "0.0.0"
  }
}
