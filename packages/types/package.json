{
  "name": "@suid/types",
  "version": "0.3.0-next.0",
  "description": "Types used by suid packages.",
  "keywords": [
    "solid",
    "solidjs",
    "components",
    "mui",
    "types"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "watch": "tsc --build -w"
  },
  "peerDependencies": {
    "solid-js": "^1.6.2"
  },
  "publishConfig": {
    "directory": "lib"
  }
}
