{
  "name": "@suid/types",
  "version": "0.1.1-next.1",
  "description": "Types used by suid packages.",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "watch": "tsc --build -w"
  },
  "peerDependencies": {
    "solid-js": "^1.3.17"
  },
  "publishConfig": {
    "directory": "lib"
  }
}
