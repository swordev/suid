{
  "name": "@suid/types",
  "version": "0.2.0",
  "description": "Types used by suid packages.",
  "keywords": [
    "solid",
    "solidjs",
    "components",
    "mui",
    "types"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "watch": "tsc --build -w"
  },
  "peerDependencies": {
    "solid-js": "^1.5.0"
  },
  "publishConfig": {
    "directory": "lib"
  }
}
