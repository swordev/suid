{
  "name": "@suid/codemod",
  "version": "0.0.4",
  "description": "Tool for migrating MUI React code to SUID SolidJS.",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "watch": "tsc --build -w"
  },
  "dependencies": {
    "ts-morph": "^14.0.0"
  },
  "publishConfig": {
    "directory": "lib"
  }
}
