{
  "name": "@suid/css",
  "version": "0.1.1-next.0",
  "description": "CSS render in JS.",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "jest",
    "watch": "tsc --build -w"
  },
  "dependencies": {
    "@suid/utils": "workspace:*"
  },
  "publishConfig": {
    "directory": "lib"
  }
}
