{
  "name": "@suid/css",
  "version": "0.1.1-next.2",
  "description": "CSS render in JS.",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "jest",
    "watch": "tsc --build -w"
  },
  "dependencies": {
    "@suid/utils": "workspace:*"
  },
  "publishConfig": {
    "directory": "lib"
  }
}
