{
  "name": "@suid/css",
  "version": "0.0.6",
  "description": "CSS render in JS.",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "jest",
    "watch": "tsc --build -w"
  },
  "dependencies": {
    "@suid/utils": "workspace:*",
    "csstype": "^3.0.11"
  },
  "publishConfig": {
    "directory": "lib"
  }
}
