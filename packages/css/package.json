{
  "name": "@suid/css",
  "version": "0.0.8",
  "description": "CSS render in JS.",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "test": "jest",
    "watch": "tsc --build -w"
  },
  "dependencies": {
    "@suid/utils": "workspace:*"
  },
  "publishConfig": {
    "directory": "lib"
  }
}
