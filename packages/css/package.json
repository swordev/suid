{
  "name": "@suid/css",
  "version": "0.1.6",
  "description": "CSS render in JS.",
  "keywords": [
    "styles",
    "css",
    "css-in-js"
  ],
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "watch": "tsc --build -w"
  },
  "dependencies": {
    "@suid/utils": "workspace:*"
  },
  "publishConfig": {
    "directory": "lib"
  }
}
